# Atenção: Toda vez que um novo endpoint for adicionado em uma das API's (simw ou mensageria)
# vai ser necessario executar o script "map_automation.py" para atualizar o mapeamento.

# Iniciar Servidor 
sudo screen -d -m -S bait-proxy node index.js